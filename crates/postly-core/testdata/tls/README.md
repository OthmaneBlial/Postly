# Postly TLS test material

The certificates and private keys in this directory are disposable fixtures
used only by local integration tests. They are not credentials and must never
be used outside the test suite.
